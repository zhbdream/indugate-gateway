# MCP Client Example

This directory will contain MCP client integration examples.

See the main documentation for API usage:

- MCP Discovery: `GET /mcp/.well-known/mcp.json`
- MCP SSE: `POST /mcp/sse`
- MCP Message: `POST /mcp/message`
